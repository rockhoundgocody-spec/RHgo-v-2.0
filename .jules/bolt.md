# Bolt's Journal

## 2025-02-15 - Initial Journal Setup
**Learning:** To avoid dependency lockfile pollution and test failure issues, dependencies (like `leaflet`) are already available via package cache or installed when building, but must be carefully managed.
**Action:** Always verify local changes compile and tests pass via pnpm commands cleanly.
